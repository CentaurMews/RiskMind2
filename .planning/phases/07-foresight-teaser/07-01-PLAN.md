---
phase: 07-foresight-teaser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - artifacts/riskmind-app/src/pages/foresight/foresight.tsx
autonomous: true
requirements:
  - FORE-01

must_haves:
  truths:
    - "Foresight page loads at its existing route without errors"
    - "Four feature cards are visible: Monte Carlo Simulation, OSINT Risk Horizon, Agent Intelligence Feed, What-If Scenario Builder"
    - "Each card shows a lucide icon, feature title, and 2-line description"
    - "Cards use a muted/preview visual treatment — clearly not live features"
    - "Page header shows 'Foresight' title and a subtitle about upcoming capabilities"
    - "Layout is 2x2 grid on desktop, single column on mobile — no broken layout at any viewport"
    - "No placeholder text, no raw 'Coming Soon' badge, no bare stub remains"
  artifacts:
    - path: "artifacts/riskmind-app/src/pages/foresight/foresight.tsx"
      provides: "Polished foresight teaser page — full replacement of bare stub"
      exports: ["default Foresight"]
  key_links:
    - from: "artifacts/riskmind-app/src/pages/foresight/foresight.tsx"
      to: "artifacts/riskmind-app/src/components/ui/card.tsx"
      via: "Card, CardHeader, CardTitle, CardContent imports"
      pattern: "from.*@/components/ui/card"
    - from: "artifacts/riskmind-app/src/pages/foresight/foresight.tsx"
      to: "lucide-react"
      via: "Dice5, Globe, Brain, GitBranch icon imports"
      pattern: "from.*lucide-react"
---

<objective>
Replace the bare "Coming Soon" stub on the Foresight page with a polished, Apple-keynote-quality 4-card feature preview that communicates the v2 roadmap with confidence and premium visual design.

Purpose: This is the final ship gate for v1.1. Demo audiences see this page and need to feel "wow — that's what's coming next." The page elevates product perception by making the roadmap tangible and desirable.
Output: A single modified foresight.tsx that is self-contained — no new files, no routes, no backend.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing stub — full replacement target -->
<!-- File: artifacts/riskmind-app/src/pages/foresight/foresight.tsx -->
Current structure:
```tsx
import { AppLayout } from "@/components/layout/app-layout";
import { Binoculars } from "lucide-react";

export default function Foresight() {
  return (
    <AppLayout>
      <div className="p-8 h-full flex flex-col items-center justify-center text-center">
        {/* bare icon + "Coming Soon" badge */}
      </div>
    </AppLayout>
  );
}
```
The entire inner content is replaced. AppLayout wrapper is preserved.

<!-- Card component exports -->
<!-- File: artifacts/riskmind-app/src/components/ui/card.tsx -->
```tsx
export { Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent }
// Card: rounded-xl border bg-card text-card-foreground shadow
// CardHeader: flex flex-col space-y-1.5 p-6
// CardTitle: font-semibold leading-none tracking-tight
// CardDescription: text-sm text-muted-foreground
// CardContent: p-6 pt-0
```

<!-- Dashboard pattern — established visual style reference -->
<!-- File: artifacts/riskmind-app/src/pages/dashboard.tsx -->
Page header pattern:
```tsx
<h1 className="text-3xl font-bold tracking-tight">Overview</h1>
<p className="text-muted-foreground mt-1">Enterprise risk and compliance posture at a glance.</p>
```
Card with gradient overlay pattern:
```tsx
<Card className="hover:shadow-md transition-shadow relative overflow-hidden">
  <div className="absolute inset-0 bg-gradient-to-br from-primary/5 to-transparent pointer-events-none" />
  {/* content with relative z-10 */}
</Card>
```
Grid pattern:
```tsx
<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build polished Foresight teaser page</name>
  <files>artifacts/riskmind-app/src/pages/foresight/foresight.tsx</files>

  <read_first>
    - artifacts/riskmind-app/src/pages/foresight/foresight.tsx — current stub (full read, replace all inner content)
    - artifacts/riskmind-app/src/components/ui/card.tsx — Card component API
    - artifacts/riskmind-app/src/pages/dashboard.tsx — established header + card gradient patterns
  </read_first>

  <action>
Replace the entire content of foresight.tsx with the implementation below. Preserve the AppLayout wrapper and default export name.

**Page structure:**
```
AppLayout
  div.p-8.max-w-5xl.mx-auto (page container, consistent with dashboard)
    Page header section
    Feature cards grid
    Footer label
```

**Page header** (match dashboard h1 style):
```tsx
<div className="mb-12">
  <h1 className="text-3xl font-bold tracking-tight">Foresight</h1>
  <p className="text-muted-foreground mt-2 max-w-xl">
    Advanced predictive capabilities are coming in the next release.
    Here's what's on the horizon.
  </p>
</div>
```

**Feature cards grid** — 2×2 on desktop, 1 col on mobile:
```tsx
<div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
  {features.map(f => <FeatureCard key={f.title} {...f} />)}
</div>
```

**Feature data array** (define above the component, outside JSX):
```tsx
const features = [
  {
    icon: Dice5,
    title: "Monte Carlo Simulation",
    description:
      "Model risk scenarios with statistical confidence. Run thousands of simulations to understand probability distributions of risk outcomes.",
  },
  {
    icon: Globe,
    title: "OSINT Risk Horizon",
    description:
      "Enrich your risk landscape with external intelligence. Automated monitoring of threat feeds, regulatory changes, and industry signals.",
  },
  {
    icon: Brain,
    title: "Agent Intelligence Feed",
    description:
      "Your autonomous risk agent's findings in one actionable inbox. Approve, dismiss, or escalate AI-detected risks with full transparency.",
  },
  {
    icon: GitBranch,
    title: "What-If Scenario Builder",
    description:
      "Explore hypothetical scenarios interactively. 'What if this vendor fails?' See cascading impacts across your risk landscape instantly.",
  },
];
```

**FeatureCard component** (defined in the same file, above the default export):
- Uses Card with `relative overflow-hidden` and a `hover:shadow-md transition-all duration-200 hover:-translate-y-0.5` for subtle lift
- Applies a glass/gradient effect using `bg-gradient-to-br from-muted/40 to-card` on the Card itself (override bg-card)
- Icon container: `h-10 w-10 rounded-lg bg-muted flex items-center justify-center mb-4` with icon at `h-5 w-5 text-muted-foreground`
- Title: `font-semibold text-base text-foreground/80` (slightly muted — preview feel)
- Description: standard `text-sm text-muted-foreground leading-relaxed mt-1`
- A "Coming in v2" pill badge at bottom-right of each card: `text-xs font-mono text-muted-foreground/60 border border-muted rounded-full px-2 py-0.5`
- No interactive elements, no onClick handlers

**Footer** (below the grid, centered):
```tsx
<p className="text-center text-xs text-muted-foreground/50 mt-12 font-mono tracking-widest uppercase">
  v2 — Planned
</p>
```

**Imports needed:**
```tsx
import { AppLayout } from "@/components/layout/app-layout";
import { Card, CardContent } from "@/components/ui/card";
import { Dice5, Globe, Brain, GitBranch } from "lucide-react";
import type { LucideIcon } from "lucide-react";
```

The FeatureCard receives `{ icon: LucideIcon; title: string; description: string }` — type it inline or with a local interface.

Do NOT use CardHeader/CardTitle — build the card internals manually inside CardContent for full layout control.
  </action>

  <verify>
    <automated>cd /home/dante/RiskMind2 && pnpm --filter riskmind-app exec tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Monte Carlo\|OSINT Risk\|Agent Intelligence\|What-If" artifacts/riskmind-app/src/pages/foresight/foresight.tsx` returns 4
    - `grep "Dice5\|Globe\|Brain\|GitBranch" artifacts/riskmind-app/src/pages/foresight/foresight.tsx` matches (all 4 icons imported)
    - `grep "Coming in v2" artifacts/riskmind-app/src/pages/foresight/foresight.tsx` matches (badge present)
    - `grep "Coming Soon" artifacts/riskmind-app/src/pages/foresight/foresight.tsx` returns nothing (old stub gone)
    - `grep "AppLayout" artifacts/riskmind-app/src/pages/foresight/foresight.tsx` matches (wrapper preserved)
    - `grep "grid-cols-1 sm:grid-cols-2" artifacts/riskmind-app/src/pages/foresight/foresight.tsx` matches (responsive grid)
    - TypeScript compile passes with no errors on the file
  </acceptance_criteria>

  <done>foresight.tsx fully replaced — 4 feature cards render, TypeScript compiles clean, old stub content absent</done>
</task>

</tasks>

<verification>
After execution:
1. `cd /home/dante/RiskMind2 && pnpm --filter riskmind-app exec tsc --noEmit` — zero errors
2. `grep -c "Monte Carlo\|OSINT Risk\|Agent Intelligence\|What-If" artifacts/riskmind-app/src/pages/foresight/foresight.tsx` — returns 4
3. `grep "Coming Soon" artifacts/riskmind-app/src/pages/foresight/foresight.tsx` — returns nothing
4. `grep "AppLayout" artifacts/riskmind-app/src/pages/foresight/foresight.tsx` — matches (route intact)
</verification>

<success_criteria>
- Foresight page renders 4 feature preview cards (Monte Carlo, OSINT Risk Horizon, Agent Intelligence Feed, What-If Scenario Builder)
- Each card carries the correct icon, title, two-line description, and a "Coming in v2" label
- Page header matches established Linear/Vercel design language (consistent with dashboard.tsx)
- No broken layouts, no placeholder text, no raw stub content remains
- TypeScript compiles without errors
- No backend changes, no new routes, no new files
</success_criteria>

<output>
After completion, create `.planning/phases/07-foresight-teaser/07-01-SUMMARY.md` using the summary template.
</output>
