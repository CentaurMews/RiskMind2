---
phase: 18-comprehensive-demo-seed-data
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - artifacts/api-server/src/lib/seed.ts
autonomous: true
requirements:
  - D-04
  - D-05
  - D-06
  - D-17
  - D-18
  - D-19

must_haves:
  truths:
    - "Assessment template library shows 3 pre-built templates"
    - "Assessments list shows 3 completed vendor assessments with scores"
    - "Assessments list shows 1 completed compliance assessment"
    - "Signal list shows 15-20 signals across 5 source types with metadata"
    - "Findings list shows 5-6 findings linked to signals and risks"
    - "Vendor scorecards show updated riskScore from assessments"
  artifacts:
    - path: "artifacts/api-server/src/lib/seed.ts"
      provides: "Seed functions for assessments, signals, and findings"
      contains: "seedCompletedAssessments"
  key_links:
    - from: "seed.ts seedCompletedAssessments"
      to: "assessmentsTable"
      via: "db.insert with templateId and contextId (vendorId) FKs"
      pattern: "db\\.insert\\(assessmentsTable\\)"
    - from: "seed.ts seedExpandedSignals"
      to: "signalsTable"
      via: "db.insert with content_hash and external_id for dedup"
      pattern: "contentHash"
    - from: "seed.ts seedFindings"
      to: "findingsTable"
      via: "db.insert with signalId and riskId FKs"
      pattern: "db\\.insert\\(findingsTable\\)"
---

<objective>
Seed assessment templates and completed assessments, expand signals across all 5 source types with realistic metadata, and create findings linked to signals and risks completing the signal->finding->risk chain.

Purpose: Populate assessment, signal, and findings pages with rich demo data showing the full traceability chain.
Output: Extended seed.ts with assessment, signal, and findings seed functions.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-comprehensive-demo-seed-data/18-CONTEXT.md
@.planning/phases/18-comprehensive-demo-seed-data/18-01-SUMMARY.md
@artifacts/api-server/src/lib/seed.ts
@lib/db/src/schema/assessments.ts
@lib/db/src/schema/signals.ts
@lib/db/src/schema/findings.ts
@lib/db/src/seed/prebuilt-templates.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed pre-built templates and completed assessments</name>
  <files>artifacts/api-server/src/lib/seed.ts</files>
  <read_first>
    - artifacts/api-server/src/lib/seed.ts (current state after Plan 01)
    - lib/db/src/seed/prebuilt-templates.ts (seedPrebuiltTemplates function signature, template titles)
    - lib/db/src/schema/assessments.ts (assessmentsTable: templateId, contextType vendor/framework, contextId, assessment_status enum draft/active/completed/abandoned, responses JSONB, score numeric, aiSummary)
    - lib/db/src/schema/vendors.ts (vendorsTable.riskScore numeric field — updated by assessment completion)
  </read_first>
  <action>
Import { seedPrebuiltTemplates } from "@workspace/db/seed/prebuilt-templates" (check the actual import path — it's `lib/db/src/seed/prebuilt-templates.ts`). Also import assessmentsTable, assessmentTemplatesTable from schema.

Create `seedAssessments()` function that:

1. Call `seedPrebuiltTemplates(tenantId)` to ensure the 3 templates exist (D-04).

2. Check assessmentsTable count === 0 for this tenant before proceeding.

3. Look up the "Vendor Security Assessment" template by title + tenantId to get its templateId.

4. Create 3 completed vendor assessments (D-05) — one for each critical/high vendor (CloudScale, DataGuard Pro, PayFlow):

   For CloudScale (critical): contextType "vendor", contextId = cloudscale vendor ID, status "completed", score "72.50", aiSummary "CloudScale demonstrates strong encryption practices (AES-256) and has a documented incident response plan tested within 12 months. Key concerns include quarterly-only privileged access reviews and 48-hour breach notification timeline. Business continuity RPO of 4 hours meets requirements. GDPR compliance is in progress. Overall vendor risk is moderate-high due to access control cadence gaps."
   Responses JSONB: create a realistic object with question IDs from the Vendor Security Assessment template (q-vs-001 through q-vs-025), e.g.:
   ```json
   {
     "q-vs-001": { "value": "true", "score": 1.0 },
     "q-vs-002": { "value": "16", "score": 0.5 },
     "q-vs-003": { "value": "quarterly", "score": 0.75 },
     "q-vs-006": { "value": "true", "score": 1.0 },
     "q-vs-007": { "value": "AES-256", "score": 1.0 },
     "q-vs-008": { "value": "true", "score": 1.0 },
     "q-vs-011": { "value": "true", "score": 1.0 },
     "q-vs-013": { "value": "6", "score": 0.75 },
     "q-vs-014": { "value": "48hrs", "score": 0.75 },
     "q-vs-016": { "value": "true", "score": 1.0 },
     "q-vs-017": { "value": "4", "score": 0.85 },
     "q-vs-018": { "value": "8", "score": 0.7 },
     "q-vs-019": { "value": "quarterly", "score": 0.75 },
     "q-vs-020": { "value": "true", "score": 1.0 },
     "q-vs-021": { "value": "true", "score": 1.0 },
     "q-vs-022": { "value": "in-progress", "score": 0.5 },
     "q-vs-023": { "value": "true", "score": 1.0 },
     "q-vs-025": { "value": "true", "score": 1.0 }
   }
   ```

   For DataGuard Pro (high): score "55.30", varied responses showing weaker posture in business continuity.
   For PayFlow (critical): score "81.20", strong responses across all sections.

5. Update vendorsTable riskScore for each vendor: riskScore = String(100 - parseFloat(score)). So CloudScale gets 27.50, DataGuard gets 44.70, PayFlow gets 18.80. Use `db.update(vendorsTable).set({ riskScore: ... }).where(eq(vendorsTable.id, vendorId))`.

6. Look up the "Compliance Control Assessment" template by title. Create 1 completed compliance assessment (D-06) linked to the ISO 27001 framework:
   contextType "framework", contextId = isoFramework.id, status "completed", score "68.40"
   aiSummary: "ISO 27001 control assessment reveals strong control design with documented objectives and assigned owners. Control automation is partial across most areas. Evidence currency needs improvement — 35% of controls have evidence older than 90 days. Two medium-severity gaps identified in access management controls with remediation plans targeting 60-day resolution."
   Include realistic responses for compliance control template question IDs (q-cc-001 through q-cc-022).

Call seedAssessments() from seedDemoDataIfEmpty() after seedExpandedVendors, passing tenantId, vendors array, and framework IDs. The function needs the ISO framework ID — retrieve it by querying frameworksTable where name = "ISO 27001:2022".
  </action>
  <verify>
    <automated>cd /home/dante/RiskMind2 && npx tsc --noEmit --project artifacts/api-server/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "seedAssessments" artifacts/api-server/src/lib/seed.ts returns >= 2 (definition + call)
    - grep -c "seedPrebuiltTemplates" artifacts/api-server/src/lib/seed.ts returns >= 1
    - grep "score.*72.50\|score.*55.30\|score.*81.20\|score.*68.40" artifacts/api-server/src/lib/seed.ts finds all 4 assessment scores
    - grep -c "riskScore" artifacts/api-server/src/lib/seed.ts returns >= 3 (vendor updates)
    - grep -c "contextType.*vendor\|contextType.*framework" artifacts/api-server/src/lib/seed.ts returns >= 4
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>3 pre-built templates seeded, 3 completed vendor assessments with realistic scores and responses, 1 completed compliance assessment, vendor riskScores updated from assessment results</done>
</task>

<task type="auto">
  <name>Task 2: Expand signals with metadata and add findings chain</name>
  <files>artifacts/api-server/src/lib/seed.ts</files>
  <read_first>
    - artifacts/api-server/src/lib/seed.ts (current state after Task 1)
    - lib/db/src/schema/signals.ts (signalsTable: source text, content text, status enum pending/triaged/finding/dismissed, classification, confidence numeric, contentHash text, externalId text, vendorId nullable FK, metadata JSONB)
    - lib/db/src/schema/findings.ts (findingsTable: signalId FK, riskId nullable FK, vendorId nullable FK, title, description, finding_status enum open/investigating/resolved/false_positive)
  </read_first>
  <action>
Import findingsTable from schema if not already imported.

Create `seedExpandedSignals()` function — REPLACE the existing 3 inline signal inserts in seedDemoDataIfEmpty() with a call to this function. The function checks signalsTable count for tenant (existing 3 signals will cause the guard to skip — so use a different guard: check if any signal has contentHash set; if none do, seed the expanded set).

Actually, better approach: keep existing 3 signals as-is (they lack contentHash). The new function checks if any signals with contentHash exist for tenant — if count === 0, insert the new ones. This way it's additive.

Add 17 new signals across all 5 source types (D-17), each with contentHash (SHA256 hex string — use realistic-looking but static hashes like "a1b2c3..." 64 chars), externalId, and metadata JSONB (D-19):

**NVD CVE signals (4):**
1. source "nvd", content "CVE-2024-21762: FortiOS out-of-bounds write vulnerability in SSL VPN. CVSS 9.8. Allows remote code execution via crafted HTTP requests.", status "triaged", classification "technology", confidence "0.95", externalId "CVE-2024-21762", vendorId null, contentHash (64 char hex), metadata: { cvssScore: 9.8, cvssVector: "CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H", cweId: "CWE-787", publishedDate: "2024-02-09", affectedProducts: ["FortiOS 7.4.0-7.4.2"] }
2. source "nvd", content "CVE-2024-3400: Palo Alto PAN-OS command injection in GlobalProtect gateway. CVSS 10.0.", status "finding", classification "technology", confidence "0.98", externalId "CVE-2024-3400", metadata: { cvssScore: 10.0, cweId: "CWE-77", publishedDate: "2024-04-12" }
3. source "nvd", content "CVE-2024-1709: ConnectWise ScreenConnect authentication bypass. CVSS 10.0.", status "triaged", externalId "CVE-2024-1709", metadata: { cvssScore: 10.0, cweId: "CWE-288" }
4. source "nvd", content "CVE-2024-27198: JetBrains TeamCity authentication bypass allowing admin access. CVSS 9.8.", status "dismissed", externalId "CVE-2024-27198", metadata: { cvssScore: 9.8, cweId: "CWE-288" }

**Shodan signals (3):**
5. source "shodan", content "Open port 3389 (RDP) detected on cloudscale.io infrastructure. Service: Microsoft Terminal Services.", status "finding", vendorId = CloudScale vendor ID, classification "technology", externalId "shodan-rdp-cloudscale", metadata: { ip: "198.51.100.42", port: 3389, service: "Microsoft Terminal Services", transport: "tcp", timestamp: "2024-11-15T08:30:00Z" }
6. source "shodan", content "Exposed MongoDB instance on port 27017 without authentication detected for dataguard.com.", status "triaged", vendorId = DataGuard vendor ID, externalId "shodan-mongo-dataguard", metadata: { ip: "203.0.113.15", port: 27017, service: "MongoDB", vulns: ["CVE-2023-44487"] }
7. source "shodan", content "SSL certificate expired on payflow.io payment gateway endpoint.", status "pending", vendorId = PayFlow vendor ID, externalId "shodan-ssl-payflow", metadata: { ip: "192.0.2.88", port: 443, certExpiry: "2024-10-01" }

**Sentinel signals (4):**
8. source "sentinel", content "Multiple failed authentication attempts detected from IP 185.220.101.x targeting Azure AD. Potential brute force attack.", status "triaged", classification "technology", externalId "sentinel-bf-20241115", metadata: { ruleId: "BruteForceAttack", severity: "High", alertCount: 47, sourceIps: ["185.220.101.42", "185.220.101.55"], targetService: "Azure AD" }
9. source "sentinel", content "Anomalous outbound data transfer of 2.4GB detected from finance-app-server to external IP during off-hours.", status "finding", classification "operational", externalId "sentinel-exfil-20241118", metadata: { ruleId: "DataExfiltration", severity: "Critical", bytesTransferred: 2400000000, destinationIp: "45.33.32.156" }
10. source "sentinel", content "Privilege escalation detected: service account granted Global Admin role outside change window.", status "triaged", externalId "sentinel-privesc-20241120", metadata: { ruleId: "PrivilegeEscalation", severity: "High", account: "svc-backup@acme.com" }
11. source "sentinel", content "Impossible travel alert: user login from New York then London within 15 minutes.", status "dismissed", externalId "sentinel-travel-20241122", metadata: { ruleId: "ImpossibleTravel", severity: "Medium" }

**MISP signals (3):**
12. source "misp", content "MISP Event #45821: APT28 campaign targeting financial services sector. 3 IP(s), 5 domain(s), 2 hash(es) shared.", status "triaged", classification "technology", externalId "misp-45821", metadata: { eventId: 45821, threatLevel: "high", orgSource: "CIRCL", tlp: "amber", attributeCount: { ip: 3, domain: 5, hash: 2 } }
13. source "misp", content "MISP Event #45903: New ransomware variant LockBit 4.0 indicators of compromise. 8 hash(es), 2 URL(s).", status "pending", externalId "misp-45903", metadata: { eventId: 45903, threatLevel: "high", orgSource: "FIRST", tlp: "green", attributeCount: { hash: 8, url: 2 } }
14. source "misp", content "MISP Event #46012: Supply chain compromise indicators for npm packages. 4 domain(s), 12 hash(es).", status "triaged", externalId "misp-46012", metadata: { eventId: 46012, threatLevel: "medium", attributeCount: { domain: 4, hash: 12 } }

**Email signals (3):**
15. source "email", content "Report from external auditor: Identified gaps in privileged access management controls during quarterly review.", status "finding", classification "compliance", externalId "email-audit-20241125", metadata: { from: "auditor@kpmg.com", subject: "Q4 Access Management Review Findings", receivedAt: "2024-11-25T09:15:00Z" }
16. source "email", content "Vendor notification: DataGuard Pro experienced a 4-hour service disruption affecting EU data centers.", status "triaged", classification "third_party", vendorId = DataGuard vendor ID, externalId "email-vendor-20241201", metadata: { from: "incidents@dataguard.com", subject: "Service Disruption Notification - EU DC", receivedAt: "2024-12-01T14:30:00Z" }
17. source "email", content "Anonymous tip: Potential insider threat activity observed in finance department file shares.", status "pending", classification "operational", externalId "email-tip-20241205", metadata: { from: "anonymous@protonmail.com", subject: "Security Concern Report", receivedAt: "2024-12-05T22:00:00Z" }

Generate unique contentHash for each: use a deterministic pattern like crypto.createHash('sha256').update(externalId).digest('hex') — or just hardcode 17 distinct 64-char hex strings for simplicity.

Create `seedFindings()` function (D-18) — add 6 findings:
1. title "Critical RDP Exposure on CloudScale Infrastructure", signalId = Shodan RDP signal, riskId = Data Breach risk (idx 1), vendorId = CloudScale, status "open"
2. title "FortiOS RCE Vulnerability Requires Immediate Patching", signalId = CVE-2024-21762 signal, riskId = Ransomware risk (idx 7), status "investigating"
3. title "Anomalous Data Exfiltration from Finance Server", signalId = Sentinel exfil signal, riskId = Data Breach risk (idx 1), status "open"
4. title "APT28 Campaign Indicators Match Internal Network Traffic", signalId = MISP APT28 signal, riskId = Ransomware risk (idx 7), status "investigating"
5. title "Privileged Access Management Gaps (External Audit)", signalId = email audit signal, riskId = Regulatory Non-Compliance (idx 2), status "resolved"
6. title "DataGuard Service Disruption Impact Assessment", signalId = email vendor signal, riskId = Supply Chain (idx 4), vendorId = DataGuard, status "open"

For findings that link to signals and risks: this completes the signal->finding->risk chain (D-18).

Call seedExpandedSignals() and seedFindings() from seedDemoDataIfEmpty() after seedAssessments(), passing tenantId, vendors, and risks arrays.
  </action>
  <verify>
    <automated>cd /home/dante/RiskMind2 && npx tsc --noEmit --project artifacts/api-server/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "seedExpandedSignals" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep -c "seedFindings" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep -c "contentHash" artifacts/api-server/src/lib/seed.ts returns >= 10
    - grep -c "externalId" artifacts/api-server/src/lib/seed.ts returns >= 10
    - grep -c "metadata" artifacts/api-server/src/lib/seed.ts returns >= 10
    - grep -c "findingsTable" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep "source.*nvd\|source.*shodan\|source.*sentinel\|source.*misp\|source.*email" artifacts/api-server/src/lib/seed.ts covers all 5 sources
    - grep -c "signalId" artifacts/api-server/src/lib/seed.ts returns >= 6 (one per finding)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>17 new signals across all 5 source types (20 total) with contentHash, externalId, and metadata JSONB; 6 findings linked to signals and risks completing the traceability chain</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit --project artifacts/api-server/tsconfig.json`
- All seed functions use add-if-empty pattern
- Signal->finding->risk chain is complete (findings reference both signalId and riskId)
- Assessment scores update vendor riskScore fields
</verification>

<success_criteria>
- 3 pre-built assessment templates seeded via seedPrebuiltTemplates()
- 3 completed vendor assessments with realistic responses and scores
- 1 completed compliance assessment linked to ISO 27001
- 17 new signals with contentHash, externalId, and metadata across all 5 source types
- 6 findings with signal->finding->risk chain complete
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-comprehensive-demo-seed-data/18-02-SUMMARY.md`
</output>
