---
phase: 18-comprehensive-demo-seed-data
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - artifacts/api-server/src/lib/seed.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-20

must_haves:
  truths:
    - "KRI trend chart on dashboard shows 90 days of historical data"
    - "Dashboard composite risk score trend displays gradual improvement with a spike 45 days ago"
    - "Seed completes without errors on a fresh database"
    - "Running seed twice does not duplicate any data"
  artifacts:
    - path: "artifacts/api-server/src/lib/seed.ts"
      provides: "Complete comprehensive seed with historical snapshots"
      contains: "seedRiskSnapshots"
  key_links:
    - from: "seed.ts seedRiskSnapshots"
      to: "riskSnapshotsTable"
      via: "db.insert with 90 daily snapshot rows"
      pattern: "db\\.insert\\(riskSnapshotsTable\\)"
---

<objective>
Seed 90 days of historical risk snapshots with a realistic composite score progression for dashboard trend charts, and verify the complete seed runs end-to-end.

Purpose: Enable the KRI trend chart and dashboard to show meaningful historical data immediately after seeding.
Output: Complete seed.ts with all data categories populated and verified.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-comprehensive-demo-seed-data/18-CONTEXT.md
@.planning/phases/18-comprehensive-demo-seed-data/18-01-SUMMARY.md
@.planning/phases/18-comprehensive-demo-seed-data/18-02-SUMMARY.md
@artifacts/api-server/src/lib/seed.ts
@lib/db/src/schema/risk-snapshots.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed 90 days of historical risk snapshots</name>
  <files>artifacts/api-server/src/lib/seed.ts</files>
  <read_first>
    - artifacts/api-server/src/lib/seed.ts (current state after Plan 02)
    - lib/db/src/schema/risk-snapshots.ts (riskSnapshotsTable: snapshotDate as date string "YYYY-MM-DD", compositeScore numeric, totalRisks integer, aboveAppetiteCount integer, cellCounts JSONB keyed by "L-I", categoryCounts JSONB keyed by category name with {score, count, highCriticalCount})
  </read_first>
  <action>
Import riskSnapshotsTable from schema if not already imported.

Create `seedRiskSnapshots()` function that generates 90 daily snapshots (D-20).

The function checks riskSnapshotsTable count === 0 for this tenant before proceeding.

**Score progression curve** (gradual improvement with a spike 45 days ago):
- Days 90-46 ago: Start at compositeScore 72, gradually decrease to 58 (improvement trend, ~0.3 per day with small random noise +/- 1.5)
- Day 45 ago: Spike to 78 (incident — the phishing campaign from incidents seed)
- Days 44-31 ago: Rapid recovery from 78 down to 62
- Days 30-1 ago: Gradual improvement from 62 to 52 (current healthy state)

For each day, compute:
- `snapshotDate`: format as "YYYY-MM-DD" string using `new Date(Date.now() - dayOffset * 86400000).toISOString().slice(0, 10)`
- `compositeScore`: String value from the curve above
- `totalRisks`: Start at 22, increase to 28 over 90 days (add 1 risk every ~15 days)
- `aboveAppetiteCount`: Correlate with compositeScore — higher score = more above appetite. Range 2-8.
- `cellCounts`: Generate a realistic distribution JSONB object. Use a base distribution that shifts slightly over time:
  ```json
  {
    "1-1": 2, "1-2": 1, "2-2": 3, "2-3": 2, "3-3": 4,
    "3-4": 3, "4-3": 2, "4-4": 2, "2-5": 1, "3-5": 2,
    "2-4": 2, "4-2": 1, "1-3": 1
  }
  ```
  Vary slightly per day (shift 1-2 counts between cells).
- `categoryCounts`: Generate per-category breakdown:
  ```json
  {
    "technology": { "score": 68, "count": 8, "highCriticalCount": 3 },
    "operational": { "score": 55, "count": 5, "highCriticalCount": 1 },
    "compliance": { "score": 52, "count": 4, "highCriticalCount": 1 },
    "financial": { "score": 48, "count": 4, "highCriticalCount": 0 },
    "strategic": { "score": 42, "count": 4, "highCriticalCount": 0 },
    "reputational": { "score": 58, "count": 3, "highCriticalCount": 1 }
  }
  ```
  Vary scores to correlate with the overall compositeScore trend. Technology should be consistently highest. During the spike, technology jumps to ~85.

Use batch insert: build an array of 90 snapshot objects and insert them all in one `db.insert(riskSnapshotsTable).values(snapshots)` call for efficiency.

The function should be deterministic (use seeded math or fixed patterns, not Math.random()) so repeated runs produce identical data. Use a simple linear interpolation + sine wave for noise: `baseScore + 2 * Math.sin(dayIndex * 0.7)`.

Call seedRiskSnapshots() from seedDemoDataIfEmpty() as the LAST seed function call (after all other data is in place), passing tenantId.
  </action>
  <verify>
    <automated>cd /home/dante/RiskMind2 && npx tsc --noEmit --project artifacts/api-server/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "seedRiskSnapshots" artifacts/api-server/src/lib/seed.ts returns >= 2 (definition + call)
    - grep -c "riskSnapshotsTable" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep -c "snapshotDate" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep -c "compositeScore" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep -c "categoryCounts" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep "90\|snapshot" artifacts/api-server/src/lib/seed.ts confirms 90 days
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>90 days of historical risk snapshots seeded with gradual improvement trend, spike at day 45, and realistic per-category breakdowns</done>
</task>

<task type="auto">
  <name>Task 2: Verify complete seed order and idempotency</name>
  <files>artifacts/api-server/src/lib/seed.ts</files>
  <read_first>
    - artifacts/api-server/src/lib/seed.ts (final state)
  </read_first>
  <action>
Review the complete seedDemoDataIfEmpty() function to verify:

1. **Seed order** respects FK dependencies (D-03):
   - tenants (existing)
   - users (existing)
   - risks (existing 10) + expanded risks (18 new)
   - vendors (existing 5) + expanded vendors (5-6 new)
   - frameworks (existing)
   - treatments (references risks + users)
   - KRIs (references risks)
   - incidents (references risks + users)
   - review cycles (references risks + users)
   - subprocessors (references vendors)
   - org dependencies (references vendors optionally)
   - monitoring configs (references assessment templates optionally)
   - risk appetite configs (standalone)
   - assessment templates via seedPrebuiltTemplates (references tenants)
   - completed assessments (references templates + vendors/frameworks)
   - signals (references vendors optionally)
   - findings (references signals + risks + vendors)
   - risk snapshots (standalone, last)

2. **Idempotency** (D-01): Every seed function uses `count(*) === 0` guard per table per tenant.

3. **Import completeness**: All table imports are present at the top of the file.

4. **Console logging**: Each seed function logs what it created with `[Seed]` prefix.

Fix any ordering issues, missing guards, or missing imports found during review. Ensure the file compiles and the seed function chain is correct.

Run the full TypeScript compilation to verify no errors.
  </action>
  <verify>
    <automated>cd /home/dante/RiskMind2 && npx tsc --noEmit --project artifacts/api-server/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "count" artifacts/api-server/src/lib/seed.ts returns >= 10 (one guard per seed function)
    - grep -c "console.log.*Seed" artifacts/api-server/src/lib/seed.ts returns >= 12
    - grep "seedExpandedRisks\|seedTreatments\|seedKRIs\|seedIncidents\|seedReviewCycles\|seedExpandedVendors\|seedSubprocessors\|seedOrgDependencies\|seedMonitoringConfigs\|seedRiskAppetiteConfigs\|seedAssessments\|seedExpandedSignals\|seedFindings\|seedRiskSnapshots" artifacts/api-server/src/lib/seed.ts | grep -c "await" returns >= 10 (all functions called with await)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Complete seed file verified: correct FK ordering, all functions idempotent, all imports present, compiles without errors</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit --project artifacts/api-server/tsconfig.json`
- Seed function call order matches FK dependency chain
- Every seed function has count === 0 guard
- 90 snapshots with realistic trend curve
</verification>

<success_criteria>
- 90 days of historical risk snapshots with gradual improvement trend and spike at day 45
- Complete seed covers all 20 decisions (D-01 through D-20)
- Seed is idempotent — running twice produces no duplicates
- TypeScript compiles cleanly
- Seed function call order respects all FK dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/18-comprehensive-demo-seed-data/18-03-SUMMARY.md`
</output>
