---
phase: 18-comprehensive-demo-seed-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - artifacts/api-server/src/lib/seed.ts
autonomous: true
requirements:
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-14
  - D-15
  - D-16

must_haves:
  truths:
    - "Risk register shows 25-30 risks across all 6 categories with mixed statuses"
    - "Treatments list shows 8-10 treatments linked to risks with all 4 strategy types"
    - "KRI dashboard shows 6-8 KRIs with some breaching thresholds"
    - "Incidents list shows 3-4 incidents linked to risks"
    - "Review cycles show 5 entries, some overdue"
    - "Vendor kanban shows 8-10 vendors across lifecycle stages"
    - "Vendor subprocessors show CloudScale with 2 subs, PayFlow with 1"
    - "Settings org dependencies show email/cloud/cdn/identity entries"
    - "Settings monitoring configs show tier-based cadence"
    - "Settings risk appetite configs show per-category thresholds"
  artifacts:
    - path: "artifacts/api-server/src/lib/seed.ts"
      provides: "Expanded seed functions for risks, treatments, KRIs, incidents, reviews, vendors, subprocessors, org deps, monitoring, appetite"
      contains: "seedExpandedRisks"
  key_links:
    - from: "seed.ts seedExpandedRisks"
      to: "risksTable"
      via: "db.insert with tenantId and ownerId FKs"
      pattern: "db\\.insert\\(risksTable\\)"
    - from: "seed.ts seedTreatments"
      to: "treatmentsTable"
      via: "db.insert with riskId FK"
      pattern: "db\\.insert\\(treatmentsTable\\)"
---

<objective>
Expand the seed dataset with 15-20 additional risks (total 25-30), treatments, KRIs, incidents, review cycles, new vendors, subprocessors, org dependencies, monitoring configs, and risk appetite configs.

Purpose: Populate all risk management and vendor ecosystem tables so every page has meaningful data for demos.
Output: Extended seed.ts with modular seed functions for each data category, called from seedDemoDataIfEmpty().
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-comprehensive-demo-seed-data/18-CONTEXT.md
@artifacts/api-server/src/lib/seed.ts
@lib/db/src/schema/risks.ts
@lib/db/src/schema/treatments.ts
@lib/db/src/schema/treatment-events.ts
@lib/db/src/schema/kris.ts
@lib/db/src/schema/incidents.ts
@lib/db/src/schema/review-cycles.ts
@lib/db/src/schema/vendors.ts
@lib/db/src/schema/vendor-subprocessors.ts
@lib/db/src/schema/org-dependencies.ts
@lib/db/src/schema/monitoring-configs.ts
@lib/db/src/schema/risk-appetite-configs.ts
@lib/db/src/schema/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand risks, add treatments, KRIs, incidents, and review cycles</name>
  <files>artifacts/api-server/src/lib/seed.ts</files>
  <read_first>
    - artifacts/api-server/src/lib/seed.ts (full current seed)
    - lib/db/src/schema/risks.ts (riskCategoryEnum, riskStatusEnum, risksTable columns)
    - lib/db/src/schema/treatments.ts (treatmentStrategyEnum uses treat/transfer/tolerate/terminate, treatmentStatusEnum)
    - lib/db/src/schema/treatment-events.ts (treatmentStatusEventsTable columns)
    - lib/db/src/schema/kris.ts (krisTable: riskId FK, warningThreshold, criticalThreshold, currentValue, unit)
    - lib/db/src/schema/incidents.ts (incidentsTable: riskId nullable, severity enum critical/high/medium/low, reportedBy, occurredAt, resolvedAt)
    - lib/db/src/schema/review-cycles.ts (reviewCyclesTable: riskId, reviewerId, review_status enum, dueDate, completedAt, notes)
  </read_first>
  <action>
Add imports for treatmentsTable, treatmentStatusEventsTable, krisTable, incidentsTable, reviewCyclesTable to seed.ts.

Create `seedExpandedRisks()` function — add 18 more risks (for 28 total) distributed across all 6 categories:
- technology: "API Gateway Rate Limiting Failure" (open, L3 I4), "Legacy System End-of-Life" (open, L4 I3), "Shadow IT Proliferation" (draft, L3 I3), "DNS Hijacking Attack" (mitigated, L2 I4), "Database Credential Exposure" (open, L3 I5)
- operational: "Business Continuity Plan Gaps" (open, L3 I4), "Inadequate Change Management" (draft, L2 I3), "Third-Party SLA Violations" (open, L4 I3)
- compliance: "GDPR Data Subject Rights Violations" (open, L3 I5), "SOX Internal Controls Weakness" (mitigated, L2 I4), "Cross-Border Data Transfer Risk" (draft, L3 I4)
- financial: "Foreign Exchange Exposure" (open, L3 I3), "Credit Counterparty Default" (accepted, L2 I4), "Insurance Coverage Gaps" (open, L3 I3)
- strategic: "Digital Transformation Delays" (open, L3 I3), "Competitor IP Litigation" (draft, L2 I5)
- reputational: "Customer Data Privacy Concerns" (open, L4 I4), "Regulatory Investigation Publicity" (mitigated, L2 I5)

Each risk uses tenantId FK and ownerId from existing users (rotate among adminUser, rmUser, roUser). Set residualLikelihood = likelihood - 1 (min 1) and residualImpact = impact - 1 (min 1) on mitigated risks.

Create `seedTreatments()` function — add 10 treatments linked to existing and new risks:
1. "Implement multi-cloud failover" (treat, in_progress, riskIdx 0 Cloud Provider Outage, cost 250000, dueDate 60 days from now)
2. "Cyber insurance policy" (transfer, completed, riskIdx 7 Ransomware, cost 85000)
3. "Accept market expansion risk" (tolerate, completed, riskIdx 8 Market Expansion)
4. "Retire legacy payment gateway" (terminate, planned, riskIdx 2 Regulatory Non-Compliance, dueDate 90 days)
5. "Deploy WAF and DDoS protection" (treat, in_progress, new risk API Gateway, cost 120000)
6. "Vendor SLA renegotiation program" (treat, planned, new risk SLA Violations, dueDate 45 days)
7. "GDPR remediation project" (treat, in_progress, new risk GDPR, cost 180000)
8. "SOX controls improvement" (treat, completed, new risk SOX, cost 95000)
9. "FX hedging strategy" (transfer, in_progress, new risk FX Exposure, cost 50000)
10. "Legacy system migration plan" (terminate, planned, new risk Legacy System, dueDate 180 days)

For each treatment, also insert a treatmentStatusEventsTable row (fromStatus null, toStatus = treatment's current status).

Create `seedKRIs()` function — add 8 KRIs linked to risks:
1. "System Uptime SLA" (riskIdx 0, warning 99.5, critical 99.0, current 99.2, unit "%") — breaching warning
2. "Mean Time to Detect" (riskIdx 7, warning 4, critical 8, current 6.5, unit "hours") — breaching warning
3. "Open Critical Vulnerabilities" (riskIdx 1, warning 5, critical 10, current 12, unit "count") — breaching critical
4. "Vendor Risk Score Average" (riskIdx 9, warning 60, critical 75, current 58, unit "score")
5. "Patch Compliance Rate" (riskIdx new Legacy System, warning 95, critical 90, current 87, unit "%") — breaching critical
6. "Failed Login Attempts (24h)" (riskIdx new DNS Hijacking, warning 100, critical 500, current 245, unit "count") — breaching warning
7. "Data Loss Events (Monthly)" (riskIdx new GDPR, warning 2, critical 5, current 1, unit "count")
8. "Regulatory Findings Open" (riskIdx 2, warning 3, critical 7, current 4, unit "count") — breaching warning

Create `seedIncidents()` function — add 4 incidents:
1. "Phishing Campaign Targeting Finance Team" (high, riskIdx 7 Ransomware, occurredAt 45 days ago, resolvedAt 42 days ago, reportedBy rmUser)
2. "CloudScale Regional Outage" (critical, riskIdx 0 Cloud Provider, occurredAt 30 days ago, resolvedAt 29 days ago, reportedBy roUser)
3. "Unauthorized Data Export Attempt" (medium, riskIdx 1 Data Breach, occurredAt 15 days ago, resolvedAt 12 days ago, reportedBy adminUser)
4. "SOX Audit Finding — Segregation of Duties" (low, new risk SOX, occurredAt 60 days ago, resolvedAt 50 days ago, reportedBy rmUser)

Create `seedReviewCycles()` function — add 5 review cycles:
1. riskIdx 0 Cloud Provider, scheduled, dueDate 10 days from now, reviewerId rmUser
2. riskIdx 1 Data Breach, overdue, dueDate 5 days ago, reviewerId rmUser
3. riskIdx 7 Ransomware, completed, dueDate 20 days ago, completedAt 18 days ago, reviewerId adminUser
4. riskIdx 2 Regulatory, overdue, dueDate 15 days ago, reviewerId roUser
5. riskIdx 4 Supply Chain, scheduled, dueDate 30 days from now, reviewerId roUser

Call all 5 functions from seedDemoDataIfEmpty() AFTER existing risk creation, using add-if-empty pattern (check count === 0 for each table before inserting). The expanded risks function should return the new risk array so treatments/KRIs/incidents can reference them by index.

Each seed function receives tenantId and relevant entity arrays (users, risks, vendors) and uses `count(*) === 0` guard per table per tenant.
  </action>
  <verify>
    <automated>cd /home/dante/RiskMind2 && npx tsc --noEmit --project artifacts/api-server/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "seedExpandedRisks" artifacts/api-server/src/lib/seed.ts returns >= 2 (definition + call)
    - grep -c "seedTreatments" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep -c "seedKRIs" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep -c "seedIncidents" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep -c "seedReviewCycles" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep -c "treatmentsTable" artifacts/api-server/src/lib/seed.ts returns >= 2 (import + insert)
    - grep -c "krisTable" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep -c "incidentsTable" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep -c "count.*=== 0" artifacts/api-server/src/lib/seed.ts returns >= 5 (one per seed function)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>18 new risks added (28 total), 10 treatments, 8 KRIs (3+ breaching thresholds), 4 incidents, 5 review cycles (2 overdue) — all inserted via add-if-empty functions called from seedDemoDataIfEmpty()</done>
</task>

<task type="auto">
  <name>Task 2: Add vendors, subprocessors, org dependencies, monitoring and appetite configs</name>
  <files>artifacts/api-server/src/lib/seed.ts</files>
  <read_first>
    - artifacts/api-server/src/lib/seed.ts (after Task 1 changes)
    - lib/db/src/schema/vendors.ts (vendorStatusEnum stages, tier enum, riskScore)
    - lib/db/src/schema/vendor-subprocessors.ts (vendorId + subprocessorId both FK to vendorsTable, criticality, discoveredBy, relationshipType)
    - lib/db/src/schema/org-dependencies.ts (category enum: email/cloud/cdn/identity/payment/communication/other, providerName, vendorId nullable, criticality)
    - lib/db/src/schema/monitoring-configs.ts (tier FK to vendorTierEnum, cadenceDays, scoreThreshold nullable, unique on tenant+tier)
    - lib/db/src/schema/risk-appetite-configs.ts (category FK to riskCategoryEnum, threshold integer default 60, unique on tenant+category)
  </read_first>
  <action>
Add imports for vendorSubprocessorsTable, orgDependenciesTable, monitoringConfigsTable, riskAppetiteConfigsTable.

Create `seedExpandedVendors()` function — add 5 more vendors (10 total) across lifecycle stages:
1. "CyberVault Security" (critical, risk_assessment, "Security", "security@cybervault.io", riskScore 72)
2. "ComplianceIQ" (high, due_diligence, "Compliance", "contact@complianceiq.com", riskScore 45)
3. "NetWatch Monitoring" (medium, contracting, "Infrastructure", "sales@netwatch.net", riskScore 38)
4. "TalentStream HR" (low, onboarding, "HR", "vendor@talentstream.co", riskScore 25)
5. "LegacyPay Services" (high, offboarding, "Payments", "admin@legacypay.com", riskScore 81)

Create `seedSubprocessors()` function — using returned vendor arrays:
- CloudScale (existing vendor index 0) → 2 subprocessors:
  - "NetWatch Monitoring" (new vendor) as subprocessor, criticality critical, discoveredBy manual, relationshipType "Infrastructure hosting"
  - Create an inline vendor "Akamai CDN Services" (medium tier, monitoring, riskScore 30) then link as subprocessor, criticality medium, discoveredBy llm, relationshipType "Content delivery"
  Wait — subprocessorId must reference vendorsTable. So create "Akamai CDN Services" as another vendor first (making 11 total), then link.
- PayFlow (existing vendor index 2) → 1 subprocessor:
  - "LegacyPay Services" (new vendor) as subprocessor, criticality high, discoveredBy manual, relationshipType "Payment processing fallback"

Create `seedOrgDependencies()` function — add 4 entries:
1. category "email", providerName "Microsoft 365", vendorId null (no matching vendor), criticality "critical"
2. category "cloud", providerName "Amazon Web Services", vendorId null, criticality "critical"
3. category "cdn", providerName "Cloudflare", vendorId null, criticality "high"
4. category "identity", providerName "Okta", vendorId null, criticality "critical"

Create `seedMonitoringConfigs()` function — add 4 configs (one per tier):
1. tier "critical", cadenceDays 7, scoreThreshold 70
2. tier "high", cadenceDays 30, scoreThreshold 60
3. tier "medium", cadenceDays 90, scoreThreshold 50
4. tier "low", cadenceDays 180, scoreThreshold null

Create `seedRiskAppetiteConfigs()` function — add 6 configs (one per risk category):
1. technology, threshold 65
2. operational, threshold 60
3. compliance, threshold 50
4. financial, threshold 55
5. strategic, threshold 70
6. reputational, threshold 60

All functions use add-if-empty (count === 0 per table per tenant). Call all from seedDemoDataIfEmpty() after the expanded risks block. seedExpandedVendors returns vendor array so seedSubprocessors can reference IDs.
  </action>
  <verify>
    <automated>cd /home/dante/RiskMind2 && npx tsc --noEmit --project artifacts/api-server/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "seedExpandedVendors" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep -c "seedSubprocessors" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep -c "seedOrgDependencies" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep -c "seedMonitoringConfigs" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep -c "seedRiskAppetiteConfigs" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep -c "vendorSubprocessorsTable" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep -c "orgDependenciesTable" artifacts/api-server/src/lib/seed.ts returns >= 2
    - grep "cadenceDays.*7" artifacts/api-server/src/lib/seed.ts finds critical tier config
    - grep "threshold.*50" artifacts/api-server/src/lib/seed.ts finds compliance appetite config
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>5 new vendors (10+ total), 3 subprocessor relationships (CloudScale->2, PayFlow->1), 4 org dependencies, 4 monitoring configs, 6 risk appetite configs all seeded via add-if-empty pattern</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit --project artifacts/api-server/tsconfig.json`
- All seed functions defined and called from seedDemoDataIfEmpty
- Add-if-empty guards present for every new table
- FK references use actual IDs from returned insert results (not hardcoded UUIDs)
</verification>

<success_criteria>
- seed.ts contains modular seed functions for expanded risks, treatments, KRIs, incidents, review cycles, expanded vendors, subprocessors, org dependencies, monitoring configs, and risk appetite configs
- All data uses realistic financial services scenario values
- All seed functions are idempotent with count === 0 guards
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-comprehensive-demo-seed-data/18-01-SUMMARY.md`
</output>
