---
phase: 14-foresight-v2
plan: 03
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - artifacts/riskmind-app/src/pages/foresight/calibration-panel.tsx
  - artifacts/riskmind-app/src/pages/foresight/scenario-compare.tsx
autonomous: true
requirements: [FRST-04, FRST-03]
must_haves:
  truths:
    - "User can click Calibrate from Real Data and see OSINT-derived FAIR parameter suggestions"
    - "Calibration shows sample size and data freshness badge"
    - "User can select two scenarios and view overlaid loss exceedance curves"
    - "Scenario comparison shows parameter diff table below the chart"
  artifacts:
    - path: "artifacts/riskmind-app/src/pages/foresight/calibration-panel.tsx"
      provides: "OSINT calibration UI with apply-to-form action"
      exports: ["CalibrationPanel"]
    - path: "artifacts/riskmind-app/src/pages/foresight/scenario-compare.tsx"
      provides: "Side-by-side scenario comparison with overlaid charts"
      exports: ["ScenarioCompare"]
  key_links:
    - from: "artifacts/riskmind-app/src/pages/foresight/calibration-panel.tsx"
      to: "POST /v1/foresight/calibrate"
      via: "Orval usePostForesightCalibrate hook"
      pattern: "calibrate\|Calibrat"
    - from: "artifacts/riskmind-app/src/pages/foresight/scenario-compare.tsx"
      to: "artifacts/riskmind-app/src/components/foresight/loss-exceedance-chart.tsx"
      via: "ComparisonChart import"
      pattern: "ComparisonChart"
---

<objective>
Build the OSINT calibration panel and scenario comparison view for the Foresight page.

Purpose: Calibration bridges real signal data to quantitative FAIR inputs ("magic" one-click calibration). Comparison enables data-driven scenario analysis.
Output: CalibrationPanel component for the Calibration tab, ScenarioCompare component for side-by-side analysis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-foresight-v2/14-CONTEXT.md
@.planning/phases/14-foresight-v2/14-RESEARCH.md
@.planning/phases/14-foresight-v2/14-01-SUMMARY.md

<interfaces>
<!-- CalibrationResult from POST /v1/foresight/calibrate (Plan 01 OpenAPI) -->
```typescript
interface CalibrationResult {
  tef: { min: number; mode: number; max: number } | null;
  vulnerability: { min: number; mode: number; max: number } | null;
  lossMagnitude: { min: number; mode: number; max: number } | null;
  sampleSize: number;
  dataFreshness: string;  // e.g., "last 90 days"
  message?: string;       // e.g., "Insufficient data (0 signals in last 90 days)"
}
```

<!-- ComparisonChart from Plan 02 loss-exceedance-chart.tsx -->
```typescript
export function ComparisonChart(props: {
  scenarios: Array<{
    name: string;
    histogram: Array<{ min: number; max: number; count: number }>;
    percentiles: Record<string, number>;
    color: string;
  }>;
}): JSX.Element;
```

<!-- ForesightScenario from Orval -->
```typescript
interface ForesightScenario {
  id: string;
  name: string;
  description?: string;
  riskId?: string;
  parameters: FAIRParams;
  calibratedFrom?: string;
  latestSimulation?: { status: string; results?: SimulationResults };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calibration panel + scenario comparison</name>
  <files>
    artifacts/riskmind-app/src/pages/foresight/calibration-panel.tsx,
    artifacts/riskmind-app/src/pages/foresight/scenario-compare.tsx
  </files>
  <read_first>
    artifacts/riskmind-app/src/pages/foresight/foresight.tsx,
    artifacts/riskmind-app/src/pages/foresight/scenario-form.tsx,
    artifacts/riskmind-app/src/components/foresight/loss-exceedance-chart.tsx,
    lib/api-client-react/src/generated
  </read_first>
  <action>
    **calibration-panel.tsx** — OSINT Calibration tab content (per D-11, D-12, D-13, FRST-04):
    - "Calibrate from Real Data" button calls `usePostForesightCalibrate` (or the appropriate Orval mutation hook for POST /v1/foresight/calibrate)
    - On success, display FAIR parameter suggestions in a readable card layout:
      - TEF suggestion: min/mode/max with "events/year" label
      - Vulnerability suggestion: min/mode/max as decimal (0-1)
      - Loss Magnitude suggestion: min/mode/max formatted as USD
    - Show "calibrated from real data" badge (per D-13) — green badge with check icon
    - Show sample size: "Based on {N} signals" and data freshness: "Data from last 90 days"
    - When calibration returns null params (insufficient data), show message: "Insufficient data — {N} signals in the last 90 days. Add signal integrations to enable calibration." with link to Settings integrations tab
    - "Apply to New Scenario" button — navigates to scenario form with calibrated values pre-filled (pass via state or callback prop)
    - Export `onCalibrate` callback prop so the scenario form's "Calibrate" button can invoke it and receive results
    - Loading state: spinner during calibration API call
    - Result card shows each FAIR parameter group with:
      - Parameter name and description
      - Suggested min/mode/max values
      - Source explanation (e.g., "Derived from 12 CVE signals, avg CVSS 7.2")

    **scenario-compare.tsx** — Scenario comparison view (per D-07):
    - Props: accepts list of scenarios with completed simulations (from parent)
    - Two-scenario picker: two dropdowns/selects to choose scenarios to compare
    - Only shows scenarios with `latestSimulation.status === "completed"`
    - Renders `<ComparisonChart>` with the two selected scenarios' histogram + percentiles data
    - Below chart: parameter diff table comparing FAIR inputs side by side
      - Rows: TEF (min/mode/max), Vulnerability (min/mode/max), Loss Magnitude (min/mode/max), Iterations
      - Columns: Scenario A name, Scenario B name, Delta (difference highlighted)
    - ALE comparison: side-by-side ALE values with percentage difference
    - Empty state: "Need at least 2 completed simulations to compare scenarios"
    - Color assignment: first scenario = primary (blue), second = destructive (red/orange)
  </action>
  <verify>
    <automated>cd /home/dante/RiskMind2 && pnpm --filter @workspace/riskmind-app build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "CalibrationPanel" artifacts/riskmind-app/src/pages/foresight/calibration-panel.tsx
    - grep -q "calibrat\|Calibrat" artifacts/riskmind-app/src/pages/foresight/calibration-panel.tsx
    - grep -q "sampleSize\|sample" artifacts/riskmind-app/src/pages/foresight/calibration-panel.tsx
    - grep -q "freshness\|Freshness\|90 days" artifacts/riskmind-app/src/pages/foresight/calibration-panel.tsx
    - grep -q "ScenarioCompare" artifacts/riskmind-app/src/pages/foresight/scenario-compare.tsx
    - grep -q "ComparisonChart" artifacts/riskmind-app/src/pages/foresight/scenario-compare.tsx
    - grep -q "diff\|Delta\|delta" artifacts/riskmind-app/src/pages/foresight/scenario-compare.tsx
  </acceptance_criteria>
  <done>Calibration panel calls calibrate endpoint, displays FAIR parameter suggestions with sample size and freshness badge, handles empty data gracefully. Scenario comparison renders overlaid loss exceedance curves with parameter diff table.</done>
</task>

<task type="auto">
  <name>Task 2: Wire calibration and comparison into foresight page tabs</name>
  <files>
    artifacts/riskmind-app/src/pages/foresight/foresight.tsx,
    artifacts/riskmind-app/src/pages/foresight/scenario-form.tsx
  </files>
  <read_first>
    artifacts/riskmind-app/src/pages/foresight/foresight.tsx,
    artifacts/riskmind-app/src/pages/foresight/calibration-panel.tsx,
    artifacts/riskmind-app/src/pages/foresight/scenario-compare.tsx,
    artifacts/riskmind-app/src/pages/foresight/scenario-form.tsx,
    artifacts/riskmind-app/src/pages/foresight/scenario-list.tsx
  </read_first>
  <action>
    **foresight.tsx** — Wire remaining tab content:
    - Calibration tab: render `<CalibrationPanel />` with callback to pre-fill scenario form
    - Add "Compare Scenarios" section within Scenarios tab (below scenario list) or as part of a toolbar — renders `<ScenarioCompare />` when user clicks "Compare" button
    - State management: when calibration provides values and user clicks "Apply to New Scenario", switch to Scenarios tab and open scenario form with pre-filled values
    - Import and render CalibrationPanel and ScenarioCompare

    **scenario-form.tsx** — Wire "Calibrate from Real Data" button:
    - Add a "Calibrate from Real Data" button in the form (per D-13)
    - When clicked, call the calibrate endpoint directly (or switch to Calibration tab)
    - If calibration returns non-null params, pre-fill the FAIR sliders with calibrated values
    - Show "calibrated from real data" badge next to the form title when values are calibrated
    - Show data freshness indicator next to the badge
  </action>
  <verify>
    <automated>cd /home/dante/RiskMind2 && pnpm --filter @workspace/riskmind-app build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "CalibrationPanel" artifacts/riskmind-app/src/pages/foresight/foresight.tsx
    - grep -q "ScenarioCompare" artifacts/riskmind-app/src/pages/foresight/foresight.tsx
    - grep -q "Calibrate from\|calibrate" artifacts/riskmind-app/src/pages/foresight/scenario-form.tsx
    - grep -q "calibrated from real data\|calibratedFrom\|calibrated" artifacts/riskmind-app/src/pages/foresight/scenario-form.tsx
  </acceptance_criteria>
  <done>Calibration tab renders CalibrationPanel. Scenario form has "Calibrate from Real Data" button that pre-fills sliders. Compare button renders ScenarioCompare with overlaid charts. All tabs wired and build passes.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @workspace/riskmind-app build` succeeds
- Calibration panel renders calibration results with sample size and freshness
- Scenario comparison overlays two loss exceedance curves
- "Calibrate from Real Data" button in scenario form pre-fills FAIR sliders
</verification>

<success_criteria>
- Calibration tab shows OSINT-derived parameter suggestions with data quality indicators
- Empty signal corpus shows clear "insufficient data" message
- Scenario comparison renders overlaid curves with parameter diff table
- Calibrated values flow from calibration to scenario form
</success_criteria>

<output>
After completion, create `.planning/phases/14-foresight-v2/14-03-SUMMARY.md`
</output>
