---
phase: 14-foresight-v2
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - artifacts/riskmind-app/src/pages/foresight/foresight.tsx
  - artifacts/riskmind-app/src/pages/foresight/scenario-list.tsx
  - artifacts/riskmind-app/src/pages/foresight/scenario-form.tsx
  - artifacts/riskmind-app/src/components/foresight/loss-exceedance-chart.tsx
autonomous: true
requirements: [FRST-01, FRST-02, FRST-03]
must_haves:
  truths:
    - "User can see a list of saved scenarios on the Scenarios tab"
    - "User can create a new scenario with FAIR-labeled min/mode/max slider inputs"
    - "User can run a simulation and see results appear automatically when complete"
    - "Completed simulation shows a loss exceedance curve with P50/P90/P99 markers"
    - "User can clone a scenario and delete a scenario"
  artifacts:
    - path: "artifacts/riskmind-app/src/pages/foresight/foresight.tsx"
      provides: "Tab-based Foresight page (Scenarios tab)"
      contains: "TabsTrigger"
    - path: "artifacts/riskmind-app/src/pages/foresight/scenario-list.tsx"
      provides: "Scenario card list with status, clone, delete"
      exports: ["ScenarioList"]
    - path: "artifacts/riskmind-app/src/pages/foresight/scenario-form.tsx"
      provides: "FAIR parameter form with triangular distribution sliders"
      exports: ["ScenarioForm"]
    - path: "artifacts/riskmind-app/src/components/foresight/loss-exceedance-chart.tsx"
      provides: "ECharts loss exceedance curve with percentile markers"
      exports: ["LossExceedanceChart"]
  key_links:
    - from: "artifacts/riskmind-app/src/pages/foresight/foresight.tsx"
      to: "Orval hooks"
      via: "useListForesightScenarios, useCreateForesightSimulation"
      pattern: "useListForesightScenarios\\|useCreateForesightSimulation"
    - from: "artifacts/riskmind-app/src/components/foresight/loss-exceedance-chart.tsx"
      to: "echarts"
      via: "ReactECharts"
      pattern: "ReactECharts"
---

<objective>
Build the core Foresight frontend: tab-based page replacing the teaser, scenario list with cards, FAIR parameter form with min/mode/max sliders, simulation trigger with auto-poll, and loss exceedance curve visualization.

Purpose: Delivers the primary user experience for creating and running Monte Carlo simulations with visual results.
Output: Working Foresight page with Scenarios tab, create/edit/clone/delete scenarios, run simulations, and view loss exceedance curves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-foresight-v2/14-CONTEXT.md
@.planning/phases/14-foresight-v2/14-RESEARCH.md
@.planning/phases/14-foresight-v2/14-01-SUMMARY.md

@artifacts/riskmind-app/src/pages/foresight/foresight.tsx
@artifacts/riskmind-app/src/pages/settings/settings.tsx
@artifacts/riskmind-app/src/components/dashboard/risk-heatmap-chart.tsx

<interfaces>
<!-- Orval hooks generated by Plan 01 (from OpenAPI spec) -->
```typescript
// From lib/api-client-react/src/generated/ — exact names depend on Orval generation
// Expected hooks:
useListForesightScenarios()
useCreateForesightScenario()
useGetForesightScenario(id)
useUpdateForesightScenario()
useDeleteForesightScenario()
useCloneForesightScenario(id)
useCreateForesightSimulation()
useGetForesightSimulation(id)
usePostForesightCalibrate()
useGetForesightScenariosTopAle()
```

<!-- FAIRParams shape from monte-carlo.ts -->
```typescript
interface FAIRParams {
  tef: { min: number; mode: number; max: number };
  vulnerability: { min: number; mode: number; max: number };
  lossMagnitude: { min: number; mode: number; max: number };
}

interface SimulationResults {
  ale: number;
  percentiles: { p5: number; p10: number; p25: number; p50: number; p75: number; p90: number; p95: number; p99: number };
  histogram: Array<{ min: number; max: number; count: number }>;
  iterations: number;
  durationMs: number;
}
```

<!-- Existing UI patterns -->
```typescript
// Tabs pattern from settings.tsx
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";

// ECharts dark mode pattern from risk-heatmap-chart.tsx
// MutationObserver on documentElement.class → themeVersion state → useMemo colors
// ReactECharts from echarts-for-react

// Slider from shadcn
import { Slider } from "@/components/ui/slider";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Foresight page shell + scenario list + scenario form</name>
  <files>
    artifacts/riskmind-app/src/pages/foresight/foresight.tsx,
    artifacts/riskmind-app/src/pages/foresight/scenario-list.tsx,
    artifacts/riskmind-app/src/pages/foresight/scenario-form.tsx
  </files>
  <read_first>
    artifacts/riskmind-app/src/pages/foresight/foresight.tsx,
    artifacts/riskmind-app/src/pages/settings/settings.tsx,
    artifacts/riskmind-app/src/components/ui/tabs.tsx,
    artifacts/riskmind-app/src/components/ui/slider.tsx,
    lib/api-client-react/src/generated
  </read_first>
  <action>
    **foresight.tsx** — Replace entire teaser page with tab-based Foresight page (per D-05):
    - Import AppLayout, Tabs/TabsContent/TabsList/TabsTrigger
    - Three tabs: "Scenarios" (default), "Simulations", "Calibration"
    - Use same TabsList styling as settings.tsx: `bg-muted/50 p-1 w-full justify-start h-12 rounded-lg`
    - Scenarios tab renders `<ScenarioList />`
    - Simulations tab: placeholder "Select a scenario to view simulations" for now (simulation detail accessed from scenario cards)
    - Calibration tab: placeholder for Plan 03
    - Page title: "Foresight" with subtitle "Monte Carlo risk simulation and quantitative analysis"

    **scenario-list.tsx** — Scenario card list component:
    - Uses Orval hook `useListForesightScenarios()` to fetch scenarios
    - Each scenario renders as a Card with:
      - Name, description (truncated), linked risk name (if riskId set)
      - Latest simulation status badge (pending/running/completed/failed) — derive from scenario response
      - ALE value if completed (formatted as USD compact)
      - "Run Simulation" button — calls `useCreateForesightSimulation` with scenarioId and iterationCount (default 50000)
      - Clone button — calls clone endpoint (per D-10)
      - Delete button with confirmation
    - "New Scenario" button opens scenario form dialog/sheet
    - Auto-poll: when any simulation has status "pending" or "running", poll scenario list every 2s (per D-08) using `refetchInterval`
    - Empty state: "No scenarios yet. Create your first scenario to run a Monte Carlo simulation."
    - When a scenario card with completed simulation is clicked, expand inline to show `<LossExceedanceChart />` (or use a Sheet/Dialog)
    - Loading: skeleton cards while fetching

    **scenario-form.tsx** — FAIR parameter input form (per D-01, D-09):
    - Form fields: name (text), description (textarea), riskId (optional select from useListRisks), iterationCount (select: 10k/25k/50k/100k)
    - FAIR parameter sliders — three groups, each with min/mode/max:
      - TEF (Threat Event Frequency): range 0-365 events/year, step 1
      - Vulnerability (P(loss)): range 0-1, step 0.01
      - Loss Magnitude: range 0-10,000,000, step 1000 (display as USD)
    - Each group shows three Radix Slider inputs (min, mode, max) with numeric labels
    - Validation: min <= mode <= max for each group
    - Tooltip on each group explaining what the parameter means in FAIR terms
    - "Calibrate from Real Data" button placeholder (wired in Plan 03)
    - Submit creates scenario via Orval hook, then optionally runs simulation immediately
    - Can be used for both create and edit (pass existing scenario data as props)
  </action>
  <verify>
    <automated>cd /home/dante/RiskMind2 && pnpm --filter @workspace/riskmind-app build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "TabsTrigger" artifacts/riskmind-app/src/pages/foresight/foresight.tsx
    - grep -q "Scenarios" artifacts/riskmind-app/src/pages/foresight/foresight.tsx
    - grep -q "Calibration" artifacts/riskmind-app/src/pages/foresight/foresight.tsx
    - grep -q "ScenarioList" artifacts/riskmind-app/src/pages/foresight/scenario-list.tsx
    - grep -q "Slider" artifacts/riskmind-app/src/pages/foresight/scenario-form.tsx
    - grep -q "tef\|TEF" artifacts/riskmind-app/src/pages/foresight/scenario-form.tsx
    - grep -q "vulnerability\|Vulnerability" artifacts/riskmind-app/src/pages/foresight/scenario-form.tsx
    - grep -q "lossMagnitude\|Loss Magnitude" artifacts/riskmind-app/src/pages/foresight/scenario-form.tsx
    - grep -q "refetchInterval\|poll" artifacts/riskmind-app/src/pages/foresight/scenario-list.tsx
  </acceptance_criteria>
  <done>Foresight page renders with three tabs. Scenarios tab shows scenario cards with CRUD operations. Scenario form has FAIR sliders with min/mode/max for TEF, Vulnerability, and Loss Magnitude. Simulation runs trigger async job and auto-poll for completion.</done>
</task>

<task type="auto">
  <name>Task 2: Loss exceedance chart + simulation detail view</name>
  <files>
    artifacts/riskmind-app/src/components/foresight/loss-exceedance-chart.tsx
  </files>
  <read_first>
    artifacts/riskmind-app/src/components/dashboard/risk-heatmap-chart.tsx,
    artifacts/riskmind-app/src/components/dashboard/kri-trend-panel.tsx
  </read_first>
  <action>
    **loss-exceedance-chart.tsx** — ECharts loss exceedance curve (per D-06, FRST-02):
    - Props: `{ histogram: Array<{min,max,count}>, percentiles: Record<string,number>, iterations: number, ale: number, title?: string, color?: string }`
    - Compute exceedance curve from histogram bins: X = loss amount ($), Y = P(loss > X) = 1 - CDF
    - ECharts `line` series with `areaStyle: { opacity: 0.2 }` and `smooth: true`
    - X-axis: "Annual Loss ($)" with USD compact formatting via `Intl.NumberFormat`
    - Y-axis: "Probability of Exceedance" from 0 to 1, formatted as percentage
    - Percentile markLines at P50, P90, P99 (vertical dashed lines with labels) from `percentiles` prop
    - Dark mode support using MutationObserver pattern from risk-heatmap-chart.tsx
    - Tooltip: hover shows "$X: Y% chance of exceeding"
    - Responsive: `style={{ width: '100%', height: 400 }}`
    - Optional `color` prop for scenario comparison overlay support (Plan 03 will use this)
    - Below chart: summary stats row showing ALE (mean), P50 (median), P90, P99, iterations, duration

    **Scenario comparison support (per D-07):**
    - Export a `ComparisonChart` component that accepts `scenarios: Array<{ name, histogram, percentiles, color }>` and renders multiple series on one chart
    - Semi-transparent fills (`areaStyle.opacity: 0.15`) so overlapping curves are visible
    - Legend showing scenario names with colors
  </action>
  <verify>
    <automated>cd /home/dante/RiskMind2 && pnpm --filter @workspace/riskmind-app build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "LossExceedanceChart" artifacts/riskmind-app/src/components/foresight/loss-exceedance-chart.tsx
    - grep -q "ReactECharts\|echarts-for-react" artifacts/riskmind-app/src/components/foresight/loss-exceedance-chart.tsx
    - grep -q "areaStyle" artifacts/riskmind-app/src/components/foresight/loss-exceedance-chart.tsx
    - grep -q "markLine" artifacts/riskmind-app/src/components/foresight/loss-exceedance-chart.tsx
    - grep -q "P50\|P90\|P99\|p50\|p90\|p99" artifacts/riskmind-app/src/components/foresight/loss-exceedance-chart.tsx
    - grep -q "MutationObserver" artifacts/riskmind-app/src/components/foresight/loss-exceedance-chart.tsx
    - grep -q "ComparisonChart" artifacts/riskmind-app/src/components/foresight/loss-exceedance-chart.tsx
  </acceptance_criteria>
  <done>Loss exceedance chart renders smooth area curve with percentile marker lines and dark mode support. ComparisonChart overlays multiple scenarios on same chart. Build passes.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @workspace/riskmind-app build` succeeds
- Foresight page replaces teaser with tab-based UI
- Scenario list shows CRUD operations with auto-poll for simulation status
- Loss exceedance chart renders from histogram data with percentile markers
</verification>

<success_criteria>
- User can navigate to Foresight page and see Scenarios/Simulations/Calibration tabs
- User can create a scenario with FAIR-labeled sliders (TEF, Vulnerability, Loss Magnitude)
- User can run a simulation and see status update automatically
- Completed simulation displays loss exceedance curve with P50/P90/P99 lines
- User can clone and delete scenarios
</success_criteria>

<output>
After completion, create `.planning/phases/14-foresight-v2/14-02-SUMMARY.md`
</output>
