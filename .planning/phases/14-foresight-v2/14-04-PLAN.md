---
phase: 14-foresight-v2
plan: 04
type: execute
wave: 3
depends_on: [14-02, 14-03]
files_modified:
  - artifacts/riskmind-app/src/components/dashboard/ale-widget.tsx
  - artifacts/riskmind-app/src/pages/dashboard.tsx
autonomous: false
requirements: [FRST-05]
must_haves:
  truths:
    - "Dashboard shows Top Risks by ALE card with top 5 risks and expected annual loss values"
    - "Empty state shows 'No simulations yet' with link to /foresight"
    - "Full Foresight flow works end-to-end: create scenario, run simulation, view results, compare"
  artifacts:
    - path: "artifacts/riskmind-app/src/components/dashboard/ale-widget.tsx"
      provides: "Top-5 ALE KPI card for dashboard"
      exports: ["AleWidget"]
    - path: "artifacts/riskmind-app/src/pages/dashboard.tsx"
      provides: "Dashboard with ALE widget integrated"
      contains: "AleWidget"
  key_links:
    - from: "artifacts/riskmind-app/src/components/dashboard/ale-widget.tsx"
      to: "GET /v1/foresight/scenarios/top-ale"
      via: "Orval useGetForesightScenariosTopAle hook"
      pattern: "topAle|top-ale|TopAle"
    - from: "artifacts/riskmind-app/src/pages/dashboard.tsx"
      to: "artifacts/riskmind-app/src/components/dashboard/ale-widget.tsx"
      via: "import AleWidget"
      pattern: "AleWidget"
---

<objective>
Add the Top Risks by ALE dashboard widget and perform end-to-end human verification of the complete Foresight v2 feature.

Purpose: The ALE widget bridges daily risk management to quantitative analysis on the main dashboard. Human verification ensures the full flow works.
Output: Dashboard ALE card, verified end-to-end Foresight flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-foresight-v2/14-CONTEXT.md
@.planning/phases/14-foresight-v2/14-01-SUMMARY.md
@.planning/phases/14-foresight-v2/14-02-SUMMARY.md
@.planning/phases/14-foresight-v2/14-03-SUMMARY.md

@artifacts/riskmind-app/src/pages/dashboard.tsx
@artifacts/riskmind-app/src/components/dashboard/kpi-card.tsx

<interfaces>
<!-- TopAleItem from Orval (generated from Plan 01 OpenAPI spec) -->
```typescript
interface TopAleItem {
  scenarioId: string;
  scenarioName: string;
  riskId?: string;
  ale: number;
}
// Hook: useGetForesightScenariosTopAle() -> { data: TopAleItem[] }
```

<!-- Dashboard grid layout from dashboard.tsx -->
```typescript
// KPI strip: 4-column grid
<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
  <KpiCard ... />  // Risks
  <KpiCard ... />  // Alerts
  <KpiCard ... />  // Vendors
  <KpiCard ... />  // Compliance (conditionally)
</div>

// Main content: 3-column grid below KPI strip
<div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
  ...
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ALE widget + dashboard integration + deploy</name>
  <files>
    artifacts/riskmind-app/src/components/dashboard/ale-widget.tsx,
    artifacts/riskmind-app/src/pages/dashboard.tsx
  </files>
  <read_first>
    artifacts/riskmind-app/src/pages/dashboard.tsx,
    artifacts/riskmind-app/src/components/dashboard/kpi-card.tsx,
    lib/api-client-react/src/generated
  </read_first>
  <action>
    **ale-widget.tsx** — Top Risks by ALE card (per D-14, D-15, FRST-05):
    - Uses Orval hook for `GET /v1/foresight/scenarios/top-ale`
    - Card with CardHeader "Top Risks by ALE" and CardContent
    - **Empty state** (per D-15): centered text "No simulations yet." with "Run First Simulation" Link to `/foresight` styled as `text-primary`
    - **Loaded state**: ordered list of top 5 items showing:
      - Rank number + scenario name (left)
      - ALE value formatted as USD compact with `/yr` suffix (right, `font-mono text-muted-foreground`)
    - Loading state: skeleton lines
    - Error state: graceful fallback (show empty state)
    - Compact design: `space-y-2` between items, `text-sm`

    **dashboard.tsx** — Integrate ALE widget:
    - Import `AleWidget` from `@/components/dashboard/ale-widget`
    - Add `<AleWidget />` in the main content area below the KPI strip
    - Place it in the 3-column grid alongside existing widgets (e.g., replace or add as 4th element, or place in a new row)
    - The widget should be in the right column or below the executive summary

    **Deploy** — Run `pnpm deploy` to rebuild frontend + backend + restart PM2.
  </action>
  <verify>
    <automated>cd /home/dante/RiskMind2 && pnpm --filter @workspace/riskmind-app build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "AleWidget" artifacts/riskmind-app/src/components/dashboard/ale-widget.tsx
    - grep -q "Top Risks by ALE\|topAle\|top-ale" artifacts/riskmind-app/src/components/dashboard/ale-widget.tsx
    - grep -q "No simulations yet\|Run First Simulation" artifacts/riskmind-app/src/components/dashboard/ale-widget.tsx
    - grep -q "/foresight" artifacts/riskmind-app/src/components/dashboard/ale-widget.tsx
    - grep -q "AleWidget" artifacts/riskmind-app/src/pages/dashboard.tsx
  </acceptance_criteria>
  <done>ALE widget renders on dashboard with top 5 risks by expected annual loss. Empty state shows "No simulations yet" with link to Foresight. Build and deploy succeed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end Foresight v2 verification</name>
  <files>artifacts/riskmind-app/src/pages/foresight/foresight.tsx</files>
  <action>
    Human verification of the complete Foresight v2 feature. No code changes — visual and functional verification only.

    What was built: Complete Foresight v2 module — Monte Carlo simulation engine, tab-based Foresight page with scenario CRUD, FAIR parameter sliders, loss exceedance curve visualization, OSINT calibration, scenario comparison, and dashboard ALE widget.

    Steps to verify:
    1. Navigate to https://app.riskmind.net/foresight — verify three tabs visible (Scenarios, Simulations, Calibration)
    2. Click "New Scenario" — verify FAIR parameter form with TEF/Vulnerability/Loss Magnitude sliders (min/mode/max each)
    3. Set TEF: 5/15/30, Vulnerability: 0.2/0.4/0.6, Loss Magnitude: 50000/200000/500000, name it "Test Scenario"
    4. Click Run Simulation — verify 202 accepted, status shows "pending" then "running" then "completed" automatically
    5. View completed simulation — verify loss exceedance curve displays with P50/P90/P99 marker lines
    6. Clone the scenario — verify new scenario "Test Scenario (Copy)" appears
    7. Edit the clone's parameters, run another simulation
    8. Try "Compare" — select both scenarios, verify overlaid curves with different colors and parameter diff table
    9. Go to Calibration tab — click "Calibrate from Real Data" — verify results show (or "insufficient data" message if no signals)
    10. Navigate to Dashboard — verify "Top Risks by ALE" card appears (with data or empty state)
    11. Check dark mode toggle — verify charts render correctly in both themes
  </action>
  <verify>
    <automated>curl -s https://app.riskmind.net/foresight | head -1</automated>
  </verify>
  <acceptance_criteria>
    - Foresight page loads with three tabs
    - Scenario CRUD works (create, clone, delete)
    - Simulation runs and completes
    - Loss exceedance curve renders with percentile markers
    - Dashboard ALE widget visible
  </acceptance_criteria>
  <done>User approves end-to-end Foresight v2 flow including scenario creation, simulation execution, result visualization, comparison, calibration, and dashboard widget.</done>
</task>

</tasks>

<verification>
- Dashboard ALE widget renders with correct empty state or data
- Full Foresight flow works: create -> simulate -> view results -> compare -> calibrate
- Dark mode works for all ECharts components
- `pnpm deploy` succeeds
</verification>

<success_criteria>
- Dashboard shows Top Risks by ALE card
- End-to-end flow verified by human: create scenario, run simulation, view curve, compare, calibrate
- All FRST requirements (01-05) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/14-foresight-v2/14-04-SUMMARY.md`
</output>
